import MesaEntryClient from "./MesaEntryClient";

export const metadata = {
    title: "Confirmar mesa",
    description: "Confirme sua mesa antes de acessar o atendimento digital."
};

export default function MesaPage() {
    return <MesaEntryClient />;
}
